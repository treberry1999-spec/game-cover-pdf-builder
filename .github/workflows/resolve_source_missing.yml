name: Resolve remaining Source covers
on:
  workflow_dispatch:
  push:
    branches: [ main ]
    paths:
      - 'magazines/resolve_source_missing.py'
      - '.github/workflows/resolve_source_missing.yml'
permissions:
  contents: read
jobs:
  fetch:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.12'}
      - run: pip install requests beautifulsoup4 pillow
      - run: python magazines/resolve_source_missing.py
      - uses: actions/upload-artifact@v4
        with:
          name: source-missing-resolution
          path: magazine_output/source_missing/
          retention-days: 7
